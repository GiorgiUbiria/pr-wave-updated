---
import { defaultLang } from '../i18n/ui';
import DefaultPage from './[lang]/index.astro';

type Props = {
  params: { lang: string };
};

const props: Props = {
  params: { lang: defaultLang },
};
---

<DefaultPage {...props} />